<% if notice %>
    <p id="notice">
      <%= notice %></p>
<% end %>

<% @products.each do |product| %>
    <div class="entry">
  <%= image_tag(product.image_url) %>
      <h3><%= product.title %></h3>
  <%= sanitize(product.description) %>
      <div class="price_line">
  Amount: <span class="price"> <%= product.amount %> </span> <br />
  Price: <span class="price"><%= number_to_currency product.price %>
  </span>
        <%= button_to 'Buy!', line_items_path(product_id: product), class: 'button' %>
      </div>
    </div>
<% end %>